# torch / torchaudio 2.7.1 (cu128, Blackwell sm_120 inclus) sont installés AVANT ce fichier par le Dockerfile.
# chatterbox-tts est installé --no-deps (pour ne pas embarquer gradio) : ses dépendances sont listées ici.

# --- Séparation instrumentale : BS-Roformer Leap Xe via bs-roformer-infer (MIT, Python >= 3.11) ---
# La roue PyPI 0.1.5 est en retard sur le dépôt : pas d'entrée Leap dans son registre, pas de
# `mlp_expansion_factor` (exigé par la config Leap Xe), pas de BSRoformerSession. On épingle le commit
# GitHub du 2026-08-17 dont le code a été relu ; à mettre à jour quand PyPI le rattrapera.
bs-roformer-infer @ git+https://github.com/openmirlab/bs-roformer-infer@b0f1386fcced25f559f3e61c9f08a73cd9bddf80

# --- Chatterbox VC (pins de chatterbox-tts 0.1.7) ---
numpy>=1.24.0,<2.0.0
librosa==0.11.0
s3tokenizer
transformers==5.2.0
diffusers==0.29.0
resemble-perth>=1.0.0
conformer==0.3.2
spacy-pkuseg
pykakasi==2.3.0
pyloudnorm
omegaconf
safetensors
huggingface_hub

# --- Visages qui parlent : LR-ASD (MIT), code vendu sous src/spark_infer/lrasd ---
# opencv HEADLESS (pas de libGL dans l'image) : décodage de l'extrait, recadrage des visages.
opencv-python-headless>=4.8,<5
# PySceneDetect : les plans, comme scene_detect() de LR-ASD (API open_video/SceneManager, 0.6+).
scenedetect>=0.6.4,<0.8
# MFCC de l'audio des pistes, exactement le paquet et les réglages de LR-ASD.
python_speech_features==0.6
# BUILD SEULEMENT : le détecteur S3FD n'est hébergé que sur Google Drive (fetch_weights.py).
gdown>=5.1

# --- Runtime ---
runpod>=1.7.0
requests
soundfile
scipy
